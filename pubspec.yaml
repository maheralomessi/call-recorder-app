name: call_recorder
description: تطبيق احترافي لتسجيل المكالمات الهاتفية
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.0
  audioplayers: ^6.0.0
  path_provider: ^2.1.3
  intl: ^0.19.0
  shared_preferences: ^2.2.3
  share_plus: ^9.0.0
  flutter_slidable: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/

